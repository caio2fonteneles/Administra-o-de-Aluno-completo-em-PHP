<?php

$matricula = $_POST["matricula"];

$linhas = [];

if (file_exists("alunos.txt")) {

    $arqAluno = fopen("alunos.txt", "r");

    while (!feof($arqAluno)) {

        $linha = fgets($arqAluno);

        if ($linha != false) {

            $dados = explode(";", trim($linha));

            if ($dados[0] != $matricula) {

                $linhas[] = $linha;

            }
        }
    }

    fclose($arqAluno);

    $arqAluno = fopen("alunos.txt", "w");

    foreach ($linhas as $linha) {

        fwrite($arqAluno, $linha);

    }

    fclose($arqAluno);
}

?>

<!DOCTYPE html>
<html>
<head>
</head>

<body>

<h1>Excluir Aluno</h1>

<p>Aluno excluído com sucesso!</p>

<a href="listarAlunos.php">Voltar para lista de alunos</a>

</body>
</html>
